#include <stdio.h>

/*
 파일: charNumber.c
 역할: 문자형(char) 변수의 값과 해당 아스키(정수) 값을 출력하는 예제
 배경지식: C에서 문자는 내부적으로 정수(아스키 코드)로 저장됩니다. `%c`는 문자, `%d`는 정수 출력 포맷입니다.
 초심자 설명: 문자 변수가 문자로 보일 때와 그 숫자값(아스키 코드)을 같이 보는 예제입니다.

 char이란 문자형 데이터를 저장할 수 있는 데이터 타입입니다. 문자란 소문자, 대문자, 숫자, 특수문자 등 하나의 글자를 말합니다.
 예를 들어, 'A', 'b', '3', '@' 등이 문자입니다. ch1, ch2, ch3는 모두 char 타입의 변수로 선언되어 있습니다.
 
*/

int main(void) // 프로그램 시작점
{
    char ch1 = 'A'; // ch1을 선언하고 문자 'A'로 초기화 (아스키 코드 65)
    char ch2 = 64;  // ch2를 선언하고 정수 리터럴 64를 저장(아스키 문자 '@')
    char ch3;       // ch3만 선언 (초기화하지 않았으므로 읽기 전에는 값이 불확실함)

    printf("ch1 : %c Number: %d\n", ch1, ch2); // ch1을 문자로, ch2를 정수로 출력 %c가 
    printf("ch2 : %c Number: %d\n", ch2, ch2); // ch2를 문자와 정수로 출력
    printf("ch3의 값을 입력하세요: ");
    scanf("%c", &ch3); // 사용자로부터 하나의 문자를 읽어 ch3에 저장 (주소 전달 필요)
    printf("ch3 : %c Number: %d\n", ch3, ch3); // 입력된 문자와 해당 아스키 값을 출력
    return 0; // 정상 종료
}
//64를 입력하면 ch3 : @ Number: 64가 출력됩니다. 54가나오는데?
//ch1과 2의 값을 출력하고 ch3의 값을 입력하라는 문구가 출력되게 하려면 어디에 넣어야 
//scanf("%c", &ch3); // 사용자로부터 하나의 문자를 읽어 ch3에 저장 (주소 전달 필요) 
// 이 줄을 printf("ch1 : %c Number: %d\n", ch1, ch2); printf("ch2 : %c Number: %d\n", ch2, ch2); 이 두 줄 사이에 넣으면 됩니다.
//ch3의 값을 입력하세요라는 문구 출력하려면 scanf("%c", &ch3); 줄 바로 위에 printf("ch3의 값을 입력하세요: "); 이렇게 하면 됩니다.
//ch1 : A Number: 64
//ch2 : @ Number: 64
//ch3의 값을 입력하세요: 64
//ch3 : 6 Number: 54
//결과값이 이렇게 나오는데 맞는지 확인해보세요. 64를 입력했을 때 ch3 : 6 Number: 54가 나오는 이유는, scanf("%c", &ch3);에서 %c는 하나의 문자만 읽어들이기 때문입니다.
// 따라서, 64를 입력하면 '6'이라는 문자만 ch3에 저장되고, '6'의 아스키 값은 54이기 때문에 ch3 : 6 Number: 54가 출력됩니다. 만약 64 전체를 읽어들이고 싶다면, %s를 사용하여 문자열로 입력받아야 합니다.
// char가 문자형 데이터를 저장하는 데이터 타입이라는데 숫자도 저장하는건 왜인가요? 라고 생각할 수 있지만, 실제로는 char 타입은 문자 데이터를 저장하는 데 주로 사용되지만, 내부적으로는 정수(아스키 코드)로 저장됩니다.
// 따라서, char 변수에 숫자를 저장할 수도 있습니다. 예를 들어, char ch = 65;라고 하면, ch에는 문자 'A'가 저장됩니다. 이는 65가 'A'의 아스키 코드이기 때문입니다.
// 데이터 타입들은 헤더를 추가해야 사용가능? 라고 생각할 수 있지만, 실제로는 char와 같은 기본 데이터 타입은 C 언어에서 별도의 헤더 파일 없이도 사용할 수 있습니다.
// 이 파일 내부에서의 각각의 줄을 이러한 순서로 작성한 이유를 설명하자면 
// 1. char ch1 = 'A'; char ch2 = 64; char ch3; 이 세 줄은 문자형 변수를 선언하고 초기화하는 부분입니다. 
// ch1은 문자 'A'로 초기화되고, ch2는 정수 64로 초기화됩니다. ch3는 선언만 되어 있고 초기화되지 않았습니다.
// 2. printf("ch1 : %c Number: %d\n", ch1, ch2); printf("ch2 : %c Number: %d\n", ch2, ch2); 이 두 줄은 ch1과 ch2의 값을 출력하는 부분입니다.
// 첫 번째 printf는 ch1을 문자로 출력하고, ch2를 정수로 출력합니다. 두 번째 printf는 ch2를 문자와 정수로 모두 출력합니다.
//